module.exports = {
  secret: 'password', // used when we create and verify JSON Web Tokens
  database: 'mongodb://127.0.0.1:27017/test', // the URI with username and password to your MongoDB installation
};
